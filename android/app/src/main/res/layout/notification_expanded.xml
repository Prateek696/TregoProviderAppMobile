<?xml version="1.0" encoding="utf-8"?>
<!-- ROOT only has the gradient background. No backgrounds on any child except action buttons. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Greeting -->
    <TextView
        android:id="@+id/notif_greeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Good Morning ☀️, ready to take a job?"
        android:textColor="#ffffff"
        android:textSize="15sp"
        android:textStyle="bold"
        android:layout_marginTop="10dp"
        android:lineSpacingMultiplier="1.15" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voice or text — no need to open the app"
        android:textColor="#94a3b8"
        android:textSize="12sp"
        android:layout_marginTop="4dp" />

    <!-- Buttons row — NO background on this container, only on the buttons themselves -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp">

        <!-- Voice: filled pill — background OK, it's a button -->
        <LinearLayout
            android:id="@+id/btn_voice"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="38dp"
            android:layout_marginEnd="5dp"
            android:background="@drawable/notif_voice_btn"
            android:orientation="horizontal"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/ic_voice"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:scaleType="centerInside" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Voice"
                android:textColor="#ffffff"
                android:textSize="13sp"
                android:textStyle="bold"
                android:paddingStart="6dp" />

        </LinearLayout>

        <!-- Text: outlined pill — background OK, it's a button -->
        <LinearLayout
            android:id="@+id/btn_text"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="38dp"
            android:layout_marginStart="5dp"
            android:background="@drawable/notif_text_btn"
            android:orientation="horizontal"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/ic_text_btn"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:scaleType="centerInside" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Text"
                android:textColor="#ffffff"
                android:textSize="13sp"
                android:textStyle="bold"
                android:paddingStart="6dp" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>

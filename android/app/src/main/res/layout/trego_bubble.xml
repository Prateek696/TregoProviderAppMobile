<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="60dp"
    android:layout_height="60dp">

    <!-- Shadow ring -->
    <View
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/bubble_shadow"
        android:elevation="8dp" />

    <!-- Bubble body (color changes during recording) -->
    <FrameLayout
        android:id="@+id/bubble_body"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:background="@drawable/bubble_bg"
        android:elevation="10dp">

        <!-- Icon (changes between logo and mic) -->
        <ImageView
            android:id="@+id/bubble_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_trego_logo"
            android:contentDescription="Record job" />
    </FrameLayout>

</FrameLayout>

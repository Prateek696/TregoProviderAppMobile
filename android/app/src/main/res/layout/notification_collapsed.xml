<?xml version="1.0" encoding="utf-8"?>
<!-- ROOT only has the gradient background. No backgrounds on any child. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical"
    android:minHeight="50dp">

    <!-- Text only — logo handled by system small icon -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/notif_greeting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Good Morning ☀️, ready to take a job?"
            android:textColor="#ffffff"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Trego • Provider Active"
            android:textColor="#ffffff"
            android:textSize="11sp"
            android:layout_marginTop="1dp" />

    </LinearLayout>

</LinearLayout>

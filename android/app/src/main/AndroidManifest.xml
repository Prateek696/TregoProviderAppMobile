<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Floating bubble foreground service -->
      <service
          android:name=".TregoBubbleService"
          android:exported="false"
          android:foregroundServiceType="specialUse" />

      <!-- Background voice recording service (notification bar) -->
      <service
          android:name=".TregoRecordingService"
          android:exported="false"
          android:foregroundServiceType="microphone" />

      <!-- Persistent job capture shortcut notification -->
      <service
          android:name=".TregoPersistentService"
          android:exported="false"
          android:foregroundServiceType="dataSync" />

      <!-- Notification action receiver (voice + text without opening app) -->
      <receiver
          android:name=".TregoActionReceiver"
          android:exported="false">
          <intent-filter>
              <action android:name="com.tregoproviderapp.ACTION_RECORD_VOICE" />
              <action android:name="com.tregoproviderapp.ACTION_RECORD_TEXT" />
              <action android:name="com.tregoproviderapp.ACTION_STOP_RECORDING" />
          </intent-filter>
      </receiver>

    </application>
</manifest>
